# 🔗 Подключение GitHub

## Шаг 1: Создайте репозиторий на GitHub

1. Зайдите на https://github.com/new
2. Заполните форму:
   - **Repository name:** `what-if-device`
   - **Description:** AI-powered alternative scenario generator
   - **Visibility:** Public или Private (на ваш выбор)
   - ⚠️ **НЕ** добавляйте README, .gitignore или лицензию (они уже есть)
3. Нажмите "Create repository"

## Шаг 2: Подключите локальный репозиторий

После создания репозитория GitHub покажет команды. Используйте эти:

```bash
# Замените YOUR_USERNAME на ваш GitHub username
git remote add origin https://github.com/YOUR_USERNAME/what-if-device.git

# Или если используете SSH
git remote add origin git@github.com:YOUR_USERNAME/what-if-device.git

# Переименуйте ветку в main (если нужно)
git branch -M main

# Отправьте код на GitHub
git push -u origin main
```

## Шаг 3: Проверьте

Обновите страницу репозитория на GitHub - вы должны увидеть все файлы!

## Готово! ✅

Теперь можете:
- Деплоить на Vercel прямо из GitHub
- Настроить автоматический деплой при push
- Работать в команде

## Следующие шаги

1. **Подключите Vercel:**
   - Зайдите на https://vercel.com
   - Нажмите "Add New Project"
   - Выберите ваш GitHub репозиторий
   - Добавьте переменные окружения
   - Деплойте!

2. **Настройте автоматический деплой:**
   - Каждый push в main будет автоматически деплоиться
   - Pull requests получат preview деплой

## Полезные команды Git

```bash
# Проверить статус
git status

# Добавить изменения
git add .

# Закоммитить
git commit -m "Описание изменений"

# Отправить на GitHub
git push

# Получить изменения
git pull

# Посмотреть историю
git log --oneline

# Создать новую ветку
git checkout -b feature/new-feature

# Переключиться на main
git checkout main
```

## Защита секретов

⚠️ **Важно:** Убедитесь что `.env.local` в `.gitignore`!

Проверьте:
```bash
git check-ignore .env.local
```

Должно вывести: `.env.local`

Если нет, добавьте в `.gitignore`:
```
.env.local
```

---

**Git репозиторий готов! 🎉**

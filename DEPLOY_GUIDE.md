# 🚀 Руководство по деплою на Vercel

## Подготовка к деплою

### 1. Проверка кода
✅ Все файлы проверены на ошибки
✅ TypeScript компилируется без ошибок
✅ Все зависимости установлены

### 2. Переменные окружения

Вам понадобятся следующие переменные:

#### Supabase
- `NEXT_PUBLIC_SUPABASE_URL` - URL вашего Supabase проекта
- `NEXT_PUBLIC_SUPABASE_ANON_KEY` - Публичный ключ Supabase
- `SUPABASE_SERVICE_ROLE_KEY` - Сервисный ключ Supabase (секретный!)

#### Hugging Face
- `HF_TOKEN` - Токен API Hugging Face

## Шаги деплоя

### Вариант 1: Через Vercel Dashboard (Рекомендуется)

1. **Подключите GitHub репозиторий:**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin YOUR_GITHUB_REPO_URL
   git push -u origin main
   ```

2. **Импортируйте проект в Vercel:**
   - Зайдите на https://vercel.com
   - Нажмите "Add New Project"
   - Выберите ваш GitHub репозиторий
   - Vercel автоматически определит Next.js

3. **Настройте переменные окружения:**
   - В настройках проекта перейдите в "Environment Variables"
   - Добавьте все переменные из `.env.local`:
     - `NEXT_PUBLIC_SUPABASE_URL`
     - `NEXT_PUBLIC_SUPABASE_ANON_KEY`
     - `SUPABASE_SERVICE_ROLE_KEY`
     - `HF_TOKEN`

4. **Деплой:**
   - Нажмите "Deploy"
   - Дождитесь завершения сборки (3-5 минут)

### Вариант 2: Через Vercel CLI

1. **Установите Vercel CLI:**
   ```bash
   npm i -g vercel
   ```

2. **Войдите в аккаунт:**
   ```bash
   vercel login
   ```

3. **Деплой:**
   ```bash
   vercel
   ```

4. **Добавьте переменные окружения:**
   ```bash
   vercel env add NEXT_PUBLIC_SUPABASE_URL
   vercel env add NEXT_PUBLIC_SUPABASE_ANON_KEY
   vercel env add SUPABASE_SERVICE_ROLE_KEY
   vercel env add HF_TOKEN
   ```

5. **Продакшн деплой:**
   ```bash
   vercel --prod
   ```

## После деплоя

### 1. Настройте Supabase

Добавьте домен Vercel в разрешенные URL:
- Зайдите в Supabase Dashboard
- Settings → API → URL Configuration
- Добавьте: `https://your-app.vercel.app`

### 2. Проверьте работу

Протестируйте основные функции:
- ✅ Регистрация/вход
- ✅ Создание сценария
- ✅ Чат с AI
- ✅ История
- ✅ Шаблоны

### 3. Настройте домен (опционально)

В настройках Vercel:
- Domains → Add Domain
- Следуйте инструкциям для настройки DNS

## Важные замечания

### Безопасность
- ⚠️ Никогда не коммитьте `.env.local` в Git
- ⚠️ `SUPABASE_SERVICE_ROLE_KEY` должен быть только в переменных окружения Vercel
- ⚠️ `HF_TOKEN` должен быть секретным

### Производительность
- Vercel автоматически оптимизирует сборку
- Edge Functions работают быстрее
- CDN кеширует статические файлы

### Мониторинг
- Логи доступны в Vercel Dashboard
- Можно настроить алерты на ошибки
- Analytics показывает статистику использования

## Troubleshooting

### Ошибка сборки
```bash
# Проверьте локально
pnpm build

# Если работает локально, проверьте переменные окружения в Vercel
```

### Ошибка 500
- Проверьте логи в Vercel Dashboard
- Убедитесь что все переменные окружения настроены
- Проверьте подключение к Supabase

### Медленная работа
- Проверьте регион деплоя (должен быть близко к пользователям)
- Оптимизируйте запросы к базе данных
- Используйте кеширование

## Полезные команды

```bash
# Просмотр логов
vercel logs

# Список деплоев
vercel ls

# Откатить деплой
vercel rollback

# Удалить проект
vercel remove
```

## Поддержка

- Документация Vercel: https://vercel.com/docs
- Документация Next.js: https://nextjs.org/docs
- Документация Supabase: https://supabase.com/docs

---

**Готово к деплою! 🎉**
